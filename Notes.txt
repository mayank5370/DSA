--------------Ch-3 RECURSION----------------
-Tail Recursion -> A function called tail recursive when the parent function has nothing to do when the child function is finished.
-> Tail recursive functions are generally faster than other recurcive functions because it doesn't have to save the last state.